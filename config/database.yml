# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: postgresql
  encoding: UTF8
  host: localhost
  username: the_wordler
  database: word_dar_dev
  pool: 5

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: UTF8
  username: the_wordler
  database: word_dar_test
  pool: 5

production:
  adapter: sqlite3
  encoding: UTF8
  username: the_wordler
  database: word_dar_prod
  pool: 5
